
CREATE TABLE ZCUBE_ZSOURCE
(
  SEQ       NUMBER  NOT NULL,
  WRITER    VARCHAR2(20 BYTE),
  REGNO     VARCHAR2(13 BYTE),
  SUBJECT   VARCHAR2(20 BYTE)
);

CREATE UNIQUE INDEX SCOTT.PK_ZCUBE_ZSOURCE ON ZCUBE_ZSOURCE (SEQ);



CREATE TABLE ZCUBE_ZTARGET
(
  SEQ       NUMBER  NOT NULL,
  WRITER    VARCHAR2(20 BYTE),
  REGNO     VARCHAR2(24 BYTE),
  SUBJECT   VARCHAR2(20 BYTE)
);

CREATE UNIQUE INDEX SCOTT.PK_ZCUBE_ZTARGET ON ZCUBE_ZTARGET (SEQ);



INSERT INTO zcube_zsource
  SELECT LEVEL
   ,'CUSNM'||LTRIM(TO_CHAR(LEVEL))
   ,'100'||TO_CHAR(LEVEL,'FM0000000000')
   ,'SUBJECT'||LTRIM(TO_CHAR(LEVEL))
  FROM DUAL
  CONNECT BY LEVEL <= 100;      

COMMIT;


